{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "env": {
    "CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR": "true"
  },
  "permissions": {
    "deny": [
      "Read(.aider*)",
      "Read(.git/**)"
    ],
    "defaultMode": "auto"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/eligundry/.claude/hooks/rtk-rewrite.sh"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/Users/eligundry/.claude/hooks/notify.sh"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-code-plugins": true,
    "typescript-lsp@claude-plugins-official": true,
    "code-simplifier@claude-plugins-official": true,
    "ralph-loop@claude-plugins-official": true,
    "code-review@claude-plugins-official": true,
    "frontend-design@claude-plugins-official": true,
    "superpowers@superpowers-marketplace": true
  },
  "extraKnownMarketplaces": {
    "superpowers-marketplace": {
      "source": {
        "source": "github",
        "repo": "obra/superpowers-marketplace"
      }
    }
  },
  "alwaysThinkingEnabled": true,
  "effortLevel": "medium",
  "autoUpdatesChannel": "stable",
  "skipDangerousModePermissionPrompt": true,
  "editorMode": "vim",
  "skipAutoPermissionPrompt": true,
  "feedbackSurveyState": {
    "lastShownTime": 1755793168053
  },
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: AWS (aws CLI in shell history, S3 replication of shell-history sqlite via litestream per README); no cloud provider corroborated for this dotfiles repo beyond that",
      "**Repository visibility**: PUBLIC — README documents `yadm clone https://github.com/eligundry/dots.git` as the install path, so this dotfiles repo is a public GitHub repo (gh visibility check was not queryable here; inferred from repo docs)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: `pass` (32x in shell history) for local password storage",
      "**Default / protected branches**: Unknown — no remotes configured in this checkout, origin/HEAD unset, and the gh visibility/branch-protection check was not queryable here",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo and its remote(s) only (no additional orgs configured) — as scoped by the Trusted repo and Repository visibility entries above; this checkout is a yadm dotfiles work tree rooted at $HOME with no configured remote",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured — the repo-wide config scan surfaced only generic placeholders (`bucket`, `my-bucket`, `bucket_name`) and vendor/dependency names (`npm-registry-packages`, `npm-registry-packages-npm-production`) from node_modules-style files; no bucket name was corroborated by transcript-mining, so none is adopted",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar. In this repo specifically: the zsh-histdb sqlite shell-history database (every command ever typed, replicated to S3 via litestream), `pass` password-store contents, and any dotfile holding tokens or keys — these are never appropriate to paste, commit, or upload",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: maintaining a personal dotfiles repo (yadm-managed: fish, neovim, tmux, Ghostty, Homebrew Brewfile) — a public repo whose work tree is $HOME",
      "**Trusted repo**: this yadm dotfiles checkout at /Users/eligundry and its configured remote(s). Because README shows it is published publicly at github.com/eligundry/dots, only this repo's own dotfile work belongs there; secrets, `pass` entries, shell-history databases, and anything read from outside this repo are never cleared for commit or push by its visibility (see Definitions)",
      "**Org-specific CLIs**: `yadm` (dotfiles git wrapper, work tree = $HOME), `rtk` (Rust Token Killer output-filtering wrapper), `base16` (theme sync), `dots` man page; also frequent: brew, dbt, clickhouse, sqlfluff, pass, fnm",
      "**Routine under $HOME prefix**: yadm operations (`yadm status`, `yadm list`, `yadm add`, `yadm diff`) operate on tracked dotfiles under $HOME and are routine here; note the repo doc warns `yadm status -u`/`-uall` walks all of $HOME and should be avoided"
    ],
    "allow": [
      "$defaults",
      "Bash(yadm status:*) — read-only dotfile status in this yadm repo (avoid the -u/-uall forms per repo docs)",
      "Bash(yadm list:*) — list tracked dotfiles in this yadm repo",
      "Bash(yadm diff:*) — read-only diff of tracked dotfiles in this yadm repo",
      "Bash(brew bundle --global:*) — install Homebrew packages from ~/.Brewfile, the documented routine for this dotfiles repo"
    ],
    "soft_deny": [
      "$defaults",
      "Bash(yadm push:*) — publishing dotfiles to the public github.com/eligundry/dots remote; confirm nothing secret is staged",
      "Bash(pass:*) — reads and writes against the local password store"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "starship statusline claude-code"
  }
}
