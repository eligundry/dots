[aws]
disabled = true

[gcloud]
disabled = true

[nodejs]
format = "[$symbol($version )]($style)"
version_format = "v${major}"

[package]
disabled = true

[php]
format = "[$symbol($version )]($style)"
version_format = "v${major}.${minor}"

[python]
disabled = false
format = "[$symbol($version )]($style)"
version_format = "v${major}.${minor}"

[rust]
format = "[$symbol($version )]($style)"
version_format = "v${major}.${minor}"

[vagrant]
disabled = true

[time]
disabled = true

[git_status]
ahead = "⇡"
behind = "⇣"
stashed = '⚑'
deleted = "✖"

[directory]
truncate_to_repo = false
